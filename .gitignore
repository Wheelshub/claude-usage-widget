# Live data snapshot (regenerated by the collector every refresh)
collector/usage.json

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Local secrets / overrides — never commit a real token
collector/config.local.json
.env
.env.*

# Build output
*.icuewidget

# Dev / tooling artifacts
.claude/
excalidraw.log
*.log

# OS cruft
Thumbs.db
desktop.ini
.DS_Store
